<%= simple_form_for([@post, @post.comments.build], :remote => true) do |f| %>
    <p>
        <%= f.input :content, placeholder: "Write a comment" %>
        <%= f.button :submit %>
    </p>
<% end %>