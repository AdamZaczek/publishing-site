<%= simple_form_for @user do |f| %>
            	<% if @user.errors.any? %>
	<div id="errors">
		<h4><%= pluralize(@user.errors.count, "error") %> prevented this post from saving:</h4>
	</div>
	<% end %>
	<div class="field">
    <%= f.input :email %>
    <%= f.input :password %>
    <%= f.button :submit %>
</div>
<% end %>